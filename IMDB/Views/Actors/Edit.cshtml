@model Actor
@{
    ViewData["Title"] = Model.FullName;
}
<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Update for @Model.FullName</h1>
        </p>
        <div class="row">
            <div class="col-md-6 offset-2">
                <form asp-action="Edit">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="mb-3 text-center">
                        <img id="ProfilePicturePreview" class="border-info rounded-circle" style="display:none; border:1px solid #ccc;  margin: 0 auto;" />
                    </div>
                    <div class="mb-3">
                        <label asp-for="ProfilePictureURL" class="form-label"></label>
                        <input asp-for="ProfilePictureURL" class="form-control" />
                        <span asp-validation-for="ProfilePictureURL" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="FullName" class="form-label"></label>
                        <input asp-for="FullName" class="form-control" />
                        <span asp-validation-for="FullName" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Bio" class="form-label"></label>
                        <input asp-for="Bio" class="form-control" />
                        <span asp-validation-for="Bio" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <input type="submit" value="Update" class="btn btn-outline-success float-end" />

                        <a class="btn btn-outline-secondary" asp-action="Index">ShowAll</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>

        function updateImagePreview(url) {
            var output = document.getElementById('ProfilePicturePreview');

            if (url && url.trim() !== "") {
                output.src = url;
                output.style.width = "200px";
                output.style.height = "170px";
                output.style.display = "block";
            } else {
                output.src = "";
                output.style.display = "none";
            }
        }

        $(document).ready(function () {
            updateImagePreview($("#ProfilePictureURL").val());

            $("#ProfilePictureURL").on("change", function () {
                updateImagePreview($(this).val());
            });
        });
    </script>
}

