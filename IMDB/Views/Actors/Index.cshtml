@model IEnumerable<Actor>
<link rel="stylesheet" href="~/css/actor-img.css" />
@{
    ViewData["Title"] = "Actors List";
}

<div class="row">
    <div class="col-md-8 offset-md-2">

        <table class="table   text-center align-middle">
            <thead>
                <tr>
                    <th>@Html.DisplayNameFor(m=>m.ProfilePictureURL)</th>
                    <th>@Html.DisplayNameFor(m => m.FullName)</th>
                    <th>@Html.DisplayNameFor(m => m.Bio)</th>
                    @if(User.Identity.IsAuthenticated && User.IsInRole("Admin")){
                           <th class="text-center">Actions</th>
                    
                    }
                    
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <td class="align-middle">
                            <img class="actor-img" src="@item.ProfilePictureURL" alt="@item.FullName" />
                        </td>
                        <td class="align-middle">@item.FullName</td>
                        <td class="align-middle">@item.Bio</td> 
                        @if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
                        {
                            <td class="text-end">
                                <a class="btn btn-outline-primary" asp-action="Edit" asp-route-id="@item.Id"><i class="bi bi-pencil-square"></i>Edit</a>
                                <a class="btn btn-outline-info" asp-action="Details" asp-route-id="@item.Id"><i class="bi bi-eye"></i>details</a>
                                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Id"><i class="bi bi-trash"></i>Delete</a>

                            </td>
                        }
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>
@await Html.PartialAsync("_CreateItem", "Actors")